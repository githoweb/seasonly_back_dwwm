{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <div class="container my-4 w-75">
        <h1 class="text-center mt-4">user n° {{ user.id }}</h1>
        <div class="d-flex justify-content-between">
            <a href="{{ path('app_back_user_index') }}" class="btn custom-btn-add">Liste des users</a>
            <div class="d-flex">
                <a href="{{ path('app_back_user_update', {'id': user.id}) }}" class="btn custom-btn-warning">Modifier</a>
                {{ include('back/user/_delete_form.html.twig') }}
            </div>
        </div>
        <table class="table table-striped table-bordered table-responsive mt-4">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ user.id }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ user.email }}</td>
                </tr>
                <tr>
                    <th>Newsletter</th>
                    <td>{{ user.newsletter }}</td>
                </tr>
                <tr>
                    <th>Created_at</th>
                    <td>{{ user.createdAt ? user.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                <tr>
                    <th>Updated_at</th>
                    <td>{{ user.updatedAt ? user.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
            </tbody>
        </table>
    </div>
