{% extends 'base.html.twig' %}

{% block title %}Modification des ingrédients{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between">
        <h3>Modification des ingrédients de la recette : {{ content.recipe.title }}</h3>
        <a class="btn custom-btn-add" href="{{ path('app_back_recipe_index') }}">Retour à la liste</a>
    </div>
    <div class="mt-2">
        {{ include('back/content/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
    {{ include('back/content/_delete_form.html.twig') }}
{% endblock %}