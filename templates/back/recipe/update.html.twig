{% extends 'base.html.twig' %}

{% block title %}Modification : {{ recipe.title }}{% endblock %}

{% block body %}
    <h1>Modification : {{ recipe.title }}</h1>

    {{ include('back/recipe/_form.html.twig', {'button_label': 'Update'}) }}
    
    <a class="btn custom-btn-add" href="{{ path('app_back_recipe_index') }}">Liste des recettes</a>
    <div style="margin-bottom: 10px;"></div>
    
    {{ include('back/recipe/_delete_form.html.twig') }}

{% endblock %}